import { GraphqlResponse } from './api-base';
export declare class GraphqlBatcherError extends Error {
    message: string;
    errors?: unknown[];
    constructor(message: string, errors?: unknown[]);
}
export declare class GraphqlBatcher {
    private timeout;
    private queries;
    submit(query: string, variables?: {
        [key: string]: any;
    }): Promise<GraphqlResponse>;
    private runBatch;
}
